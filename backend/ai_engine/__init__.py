"""
Dhatree AI Engine Layer.
Completely decoupled from Django views and models.
Provides standardized inference interfaces (`PredictorInterface`, `AnalyzerInterface`),
a thread-safe model registry (`ModelsRegistry`), and domain-specific ML/vision pipelines.
"""
